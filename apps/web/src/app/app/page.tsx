/**
 * File purpose: Defines the Next.js page route or route layout.
 * Main declarations: AppPage handles app page.
 */

import { ChatWorkspace } from "@/components/chat";

export default function AppPage() {
  return <ChatWorkspace />;
}
