[project]
name = "agent-runtime"
version = "0.1.0"
description = "Agent runtime adapters for WebAgent."
requires-python = ">=3.11"
dependencies = [
  "httpx>=0.27.0",
]

[tool.setuptools.packages.find]
include = ["agent_runtime", "agent_runtime.*"]
